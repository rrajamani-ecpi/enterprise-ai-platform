namespace EnterpriseAIPlatform.Application.Authorization;

/// <summary>The set of roles an Entra group can map to (spec 002 FR-004).</summary>
public enum RoleName
{
    Admin,
    Employee,
    Contractor,
    Student,
}
